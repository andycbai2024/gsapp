# 案件全流程交付验收报告

- 验收日期：2026-08-09
- 平台地址：`http://192.168.0.110`
- 真实设备：LM700 谈话主机，`192.168.0.234`
- 平台设备标识：`LM700-192-168-0-234`
- 验收范围：真实设备案件下发、会话控制、平台录像证据、网页笔录、办结、材料归档及审计追溯
- 样例性质：模拟验收案例；会话时长 19 秒，仅用于验证状态流转、证据绑定和归档链路，不代表业务讯问的建议时长

## 1. 交付结论

本次以真实网络、真实 LM700 和平台实际录像完成一宗模拟案件的全流程闭环。案件已经归档，设备侧历史记录、平台案件详情和审计记录相互一致，可作为客户验收样例保留。

视频证据由平台拉取 LM700 实时流后统一录制和校验；LM700 只接收开始、停止命令并回执执行结果，不上传设备端录像文件。

## 2. 已归档验收样例

| 项目 | 值 |
| --- | --- |
| 案件编号 | `SIM-CASE-20260809024640` |
| 案件名称 | 模拟全流程案件（自动验收） |
| 案件状态 | `archived` |
| 设备分配 | ID `8`，状态 `completed` |
| 会话编号 | `SIM-CASE-20260809024640-S1` |
| 会话状态 | `ended` |
| 开始时间 | `2026-08-09T02:47:02+00:00` |
| 结束时间 | `2026-08-09T02:47:21+00:00` |
| 笔录 | 模拟讯问笔录，已定稿，归档版本 V1 |
| 平台录像 | `live/device234_hd/2026-08-09/2026-08-09-10-47-02-0.mp4` |
| 录像 SHA-256 | `dff8ea6d6fd812a1fb4a5b012aa38e7ac49be354e43f13c8ffc17787a755a52e` |

## 3. 验收结果

| 编号 | 验收项 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| C-01 | 案件录入与设备下发 | 平台创建案件并下发至 `LM700-192-168-0-234`；设备确认接收 | 通过 |
| C-02 | 办案会话排期与启动 | 平台创建会话并向 LM700 发送 `recordStart`；设备返回 `returnVal:0` | 通过 |
| C-03 | 平台统一录像 | 平台流 `live/device234_hd` 在会话窗口生成实际 MP4 | 通过 |
| C-04 | 停止办案 | 平台发送 `recordCease`；设备返回 `returnVal:0`，会话转为 `ended` | 通过 |
| C-05 | 录像证据绑定 | 自动扫描任务绑定 MP4，完整性状态为 `verified`，保存 SHA-256 | 通过 |
| C-06 | 笔录 | 创建、保存、生成 V1 HTML 归档版本并定稿 | 通过 |
| C-07 | 办结完整性校验 | 设备完成、会话结束、笔录定稿、录像证据、审计链等检查全部通过 | 通过 |
| C-08 | 办结与归档 | 案件先转为 `closed`，随后转为 `archived` | 通过 |
| C-09 | 设备侧历史展示 | LM700 工作台显示“已完成 / 已结束 / 已归档” | 通过 |

## 4. 审计与材料完整性

本案件的办结完整性报告返回 `ready:true`。以下检查均为 `true`：

- 已分配设备，且全部设备分配完成。
- 所有办案会话均已结束。
- 所有笔录均已定稿。
- 所有登记材料均已绑定。
- 所有已结束会话均有已校验的平台录像证据。
- 同步刻录校验项通过。
- 审计哈希链有效。

关键审计事件按顺序包括：平台录像绑定（`recording.bound`）、案件办结（`case.closed`）和案件归档（`case.archived`）。

## 5. 客户复验步骤

1. 以管理员身份登录平台，打开案件管理，按案件编号 `SIM-CASE-20260809024640` 查询。
2. 在案件详情确认案件状态为“已归档”，设备分配为“已完成”，会话为“已结束”。
3. 查看笔录，确认“模拟讯问笔录”已定稿且存在 V1 归档版本。
4. 在录像回放中选择 `live/device234_hd` 和日期 `2026-08-09`，播放上述 MP4。
5. 在保存录像的 Linux 主机上校验文件哈希；结果应与本报告第 2 节一致：

	```bash
	sha256sum /opt/media/bin/www/record/live/device234_hd/2026-08-09/2026-08-09-10-47-02-0.mp4
	```

6. 在案件审计记录中确认 `recording.bound`、`case.closed`、`case.archived` 事件及哈希链。
7. 登录 LM700 办案工作台，在“历史下发”中确认同一案件显示“已完成 / 已结束 / 已归档”。

## 6. 办结与归档操作规则

办理中的案件只能在完整性报告返回 `ready:true` 后办结。平台依次执行以下两个管理员接口，第二个接口只接受状态为 `closed` 的案件：

```http
POST /api/cases/{case_id}/close
POST /api/cases/{case_id}/archive
```

客户应通过案件管理页面完成上述操作；接口仅用于集成或验收自动化，必须使用管理员会话，且不得跳过完整性校验。归档后案件材料进入只读状态。

## 7. 部署与运行检查

平台容器编排位于 `docker/docker-compose.yaml`，包含 Web/FastAPI 服务与 ZLMediaKit，并将平台录像目录挂载为 `/opt/media/bin/www/record`。交付环境需保证：

- LM700 可以访问平台 HTTP 接口与平台媒体服务端口。
- 平台可以访问 LM700 的 RTSP 地址 `rtsp://192.168.0.234:554/live/hd`。
- 平台录像目录可写且能够持久化保存。
- 生产环境已设置并妥善保管设备接入密钥、管理员密码与 `STREAMUI_AUTH_SECRET`。
- 生产入口使用 HTTPS，证书、反向代理和安全组策略由客户部署环境统一管理。
- 设备接入密钥和管理员密码遵循客户密码策略轮换；轮换后需同步更新授权设备配置。
- 定期备份平台 SQLite 数据库与 `/opt/media/bin/www/record` 录像目录，并在客户指定恢复环境执行恢复演练。

本次验收前已执行后端 Python 语法编译检查：

```powershell
.\.venv\Scripts\python.exe -m compileall -q backend
```

结果：通过。

## 8. 交付边界

本报告确认“平台统一案件与材料归档”闭环已交付。设备端本地录像上传不属于本方案范围；视频证据以平台录制、平台文件哈希和案件审计链为准。

报警硬件输入、门禁、LED、云台、抓图和远程语音提审等独立外设能力，应在接入相应硬件协议、控制器或媒体终端后单独验收，不作为本案件闭环交付的前置条件。